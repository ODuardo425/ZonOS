# ── Supabase ──
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# ── JWT (gere com: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))")
JWT_SECRET=troque_por_uma_string_aleatoria_longa

# ── Servidor ──
PORT=3000

# ── Frontend (CORS) ──
# Separe múltiplas origens com vírgula
ALLOWED_ORIGINS=https://seu-app.vercel.app,http://localhost:5500
